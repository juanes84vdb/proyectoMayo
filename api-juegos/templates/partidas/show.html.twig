{% extends 'base.html.twig' %}

{% block title %}Partidas{% endblock %}

{% block body %}
    <h1>Partidas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partida.id }}</td>
            </tr>
            <tr>
                <th>Acabada</th>
                <td>{{ partida.acabada ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Turno</th>
                <td>{{ partida.turno ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_partidas_index') }}">back to list</a>

    <a href="{{ path('app_partidas_edit', {'id': partida.id}) }}">edit</a>

    {{ include('partidas/_delete_form.html.twig') }}
{% endblock %}
